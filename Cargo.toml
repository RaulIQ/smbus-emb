# This file was automatically generated.

[package]
edition = "2024"
name = "smbus-emb"
version = "0.1.0"

[dependencies]
cortex-m = { version = "0.7.7", features = [
    "inline-asm",
    "critical-section-single-core",
] }
cortex-m-rt = "0.7.5"
defmt = { version = "1.0.1", optional = true }
defmt-rtt = { version = "1.1.0", optional = true }
embassy-executor = { path = "../my-embassy/embassy-executor", features = [
    "platform-cortex-m",
    "executor-thread",
] }
embassy-stm32 = { path = "../my-embassy/embassy-stm32", features = [
    "memory-x",
    "stm32f103c8",
    "time-driver-any",
    "exti",
    "unstable-pac",
] }
embassy-time = { path = "../my-embassy/embassy-time", features = [
    "tick-hz-32_768",
] }
panic-halt = "1.0.0"
panic-probe = { version = "1.0.0", features = ["print-defmt"], optional = true }

[[bin]]
name = "smbus-emb"
test = false
bench = false

[profile.dev]
debug = true
lto = true
opt-level = "z"
incremental = true

[profile.release]
debug = false
lto = true
opt-level = "z"
incremental = true

[features]
defmt = ["dep:defmt"]
defmt-rtt = ["dep:defmt-rtt"]
panic-probe = ["dep:panic-probe"]
default = ["debug"]
debug = [
    "defmt",
    "defmt-rtt",
    "panic-probe",
    "embassy-executor/defmt",
    "embassy-time/defmt",
    "embassy-time/defmt-timestamp-uptime",
    "embassy-stm32/defmt",
]
